---
# nethermind-lodestar scenario configuration
# This file was generated from a template

dependency:
  name: galaxy

driver:
  name: docker

platforms:
  - name: ephemery-nethermind-lodestar
    image: geerlingguy/docker-ubuntu2204-ansible:latest
    pre_build_image: true
    privileged: true
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:ro
      - /var/run/docker.sock:/var/run/docker.sock:rw

provisioner:
  name: ansible
  inventory:
    group_vars:
      all:
        el: nethermind
        cl: lodestar

verifier:
  name: ansible

scenario:
  name: clients/nethermind-lodestar
  test_sequence:
    - dependency
    - lint
    - cleanup
    - destroy
    - syntax
    - create
    - prepare
    - converge
    - idempotence
    - verify
    - cleanup
    - destroy
