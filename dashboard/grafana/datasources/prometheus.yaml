---
# config file version
apiVersion: 1

# list of datasources to insert/update
datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    orgId: 1
    url: http://prometheus:9090
    basicAuth: false
    isDefault: true
    editable: false
    version: 1
